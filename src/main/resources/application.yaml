spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://localhost:5432/crealink_db
    username: crealink_db
    password: 011832
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

security:
  jwt:
    secret-key: f+rL3GDJymQlWku1u+QTp6MOm7802y8lsMcKDZrFfrU=
    expiration-time: 2592000000

logging:
  level:
    org.flywaydb: DEBUG